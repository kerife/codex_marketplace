# Private first-interview practice feedback v1 implementation plan

- [x] Add failing tests for exact handoff identity, ephemeral answer,
  categorical feedback, generic rejection, schema closure, and HTML redaction.
- [x] Implement the opaque feedback identity and deterministic builder.
- [x] Reorder sourced practice HTML so action and review precede the origin
  receipt; preserve the existing static accessibility boundaries.
- [x] Update root/plugin routing docs, interview skill guidance, and the
  Superdesign design-system note.
- [ ] Run the full applicable source/package/release checks and record any
  inherited timeout separately.
- [ ] Bump the plugin version, reinstall the exact cache row, update immutable
  attestation, push `HEAD:main`, and verify remote parity.
